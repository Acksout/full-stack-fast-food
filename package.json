{
  "name": "full-stack-fast-food",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run dev\"",
    "server": "cd backend && npm run server",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "lint": "cd frontend && npm run lint"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}